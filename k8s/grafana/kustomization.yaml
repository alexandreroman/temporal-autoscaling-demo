apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: monitoring
configMapGenerator:
  - name: grafana-dashboard-autoscaling-demo
    options:
      labels:
        grafana_dashboard: "1"
    files:
      - dashboard.json
